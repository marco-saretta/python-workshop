[project]
name = "python-workshop"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "ipykernel>=7.2.0",
    "numpy>=2.4.2",
    "pytest>=9.0.2",
    "ruff>=0.15.2",
]


[tool.ruff]
line-length = 120

